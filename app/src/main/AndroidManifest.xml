<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exercise2_a">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Exercise2_A">
        <activity android:name=".LihatDataTeman"
            android:parentActivityName=".MainActivity"
            android:label="@string/labelLihatData">

        </activity>
        <activity
            android:name=".EditTeman"
            android:label="@string/labetEditData">

        </activity>
        <activity
            android:name=".TemanBaru"
            android:label="@string/labetInsertData"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>